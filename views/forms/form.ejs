<%- include("../head.partial.ejs") %>
<h1><%= title %></h1>
<% if (user) {%>
<p><%= description %></p>
  <form action="/" method="POST">
    <input
      type="text"
      name="formText"
      id="formText"
      required
      minlength="1"
      maxlength="500"
      />
      
    <button type="submit">Dodaj</button>
  </form>
</ul><br>
<a href="/logout" ><button type="button" class="butt1">Wyloguj</button></a>
<% } else { %>
  <p>Zaloguj się, aby dodawać wiadomości.</p>
  <a href="/login" ><button type="button" class="butt1">Zaloguj się</button></a>
  <a href="/register" ><button type="button" class="butt1">Utwórz nowe konto</button></a>
<% } %>

<h3>Wiadomości:</h3>
<ul>
    <% items.forEach(msg => { %>
      <% if (user && (user.role === "admin" || user.id === msg.user_id)) { %>
        <li><form action="/edit" method="POST" class="editdel">
        <input type="hidden" name="id" value="<%= msg.id %>">
        <input
          type="text"
          name="message"
          value="<%= msg.message %>"
          required
          minlength="1"
          maxlength="500"
        />
        <button type="submit" class="butt1">Zapisz</button>
      </form>

      <form action="/delete" method="POST" class="editdel">
        <input type="hidden" name="id" value="<%= msg.id %>">
        <button type="submit" class="butt1">Usuń</button>
      </form></li>
        <% } else { %>
          <li>
          <%= msg.message %>
          </li>
        <% } %>
    <% }) %>
    </ul>
<%- include("../foot.partial.ejs") %>